# 🚀 Real ENTSO-E Data Is Ready

You already have ~3 years of real prices + weather + load. Use this checklist only if you need to refresh.

## Fast Path (API, recommended)

```powershell
python scripts/fetch_entsoe_data.py ^
  --areas DE_LU FR IT ES NL BE ^
  --start-date 2023-01-01 --end-date 2025-12-31 ^
  --chunk-days 60 --merge-existing
```

Then verify:
```powershell
python check_data.py
.venv\Scripts\pytest -q   # 87 tests
```

## GUI Fallback (if token unavailable)
- Download 2024 + 2023 from https://www.entsoe.eu/data/energy-prices-data/ (Day-ahead prices, DE)
- Run:
  ```powershell
  python scripts/merge_years.py --input1 GUI_2024.csv --input2 GUI_2023.csv --output data\DE_LU\day_ahead.csv --sequence 1
  python check_data.py
  .venv\Scripts\pytest -q
  ```

## Quick Links
- Guide: `REAL_DATA_DOWNLOAD.md`
- Validator: `python check_data.py`
- Inventory: `python scripts/summarize_datasets.py`
- API help: `python scripts/diagnose_entsoe.py`

**Result:** Production-ready ENTSO-E dataset refreshed with one command. ✅
